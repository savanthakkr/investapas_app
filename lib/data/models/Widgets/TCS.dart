
import 'dart:ui';

/// TCS model For Time Count Subtitle
class TCS{
  /// title,counts and subtitle  of the widget
  String title,count,subtitle;
  /// widget color
  Color color;
  /// constructor of TCS
  TCS({required this.color,required this.title,required this.count,required this.subtitle});
}
