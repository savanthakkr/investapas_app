import 'package:flutter/material.dart';

/// Language model
class LanguageModel {
  /// locale
  final Locale locale;
  /// name
  final String name;
  
/// constructor
  const LanguageModel({

    required this.locale,
    required this.name,
  
  });
}
